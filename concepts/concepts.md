## NOTE: there is no READme or javascript file for this prompt. Just answer each question below in your own words and be as thorough as possible.

1. When would you use a for loop and when would you use a for-in loop?
I would use a for-loop when looping through objects. I would use a for-in loop when looping through arrays.

2. What is an anonymous function? Why would we want to use one?
An anonymous function is a function that is not named and can only be used once unless it is stored in a variable.

3. What is an IIFE, and how is it written?
IIFE stands for Immediated Invoked Function Expression which is an anonymous function that often takes form in a higher order function.

4. Why do we write tests for our code?
We write tests to defined the expected outcomes of our code.

5. What does it mean to clone a repo?
To clone a repo means to create a copy of an existing repo and access it from your Github workspace.

6. What is a higher order function? What is a callback?
A higher order function is a function that takes in another function as a parameter. A callback is a paramater that references or calls upon a function outside of the given one to execute a block of code, often another function.